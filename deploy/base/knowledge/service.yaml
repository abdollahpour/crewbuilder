apiVersion: v1
kind: Service
metadata:
  name: knowledge
  labels:
    app.kubernetes.io/name: knowledge
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/name: knowledge
  ports:
    - name: http
      port: 8080
      targetPort: http
