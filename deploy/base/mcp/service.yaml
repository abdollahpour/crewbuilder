apiVersion: v1
kind: Service
metadata:
  name: mcp
  labels:
    app.kubernetes.io/name: mcp
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/name: mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
