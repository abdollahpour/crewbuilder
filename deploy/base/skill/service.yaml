apiVersion: v1
kind: Service
metadata:
  name: skill
  labels:
    app.kubernetes.io/name: skill
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/name: skill
  ports:
    - name: http
      port: 8080
      targetPort: http
