apiVersion: v1
kind: Service
metadata:
  name: crew
  labels:
    app.kubernetes.io/name: crew
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/name: crew
  ports:
    - name: http
      port: 8080
      targetPort: http
